map $http_x_forwarded_proto $upstream_forwarded_proto {
    default $http_x_forwarded_proto;
    "" $scheme;
}

map $http_x_real_ip $upstream_real_ip {
    default $http_x_real_ip;
    "" $remote_addr;
}

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml
        application/xml+rss
        application/wasm
        image/svg+xml;
    gzip_min_length 256;

    # API proxy
    location ^~ /api/ {
        proxy_pass http://api:8080;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 600s;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $upstream_real_ip;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $upstream_forwarded_proto;
    }

    # MinIO 文件代理（预签名 URL 经由此路径访问）
    location ^~ /minio/ {
        proxy_pass http://minio:9000/;
        # MinIO presigned URLs are generated against the internal endpoint.
        # Preserve that signed Host while exposing the URL through /minio/.
        proxy_set_header Host minio:9000;
        proxy_set_header X-Real-IP $upstream_real_ip;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $upstream_forwarded_proto;
    }

    # Flutter web app - SPA 路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Flutter 启动入口和 Service Worker 必须重新验证，避免新部署被旧壳文件锁住。
    location = /flutter_bootstrap.js {
        add_header Cache-Control "no-cache";
    }

    location = /flutter_service_worker.js {
        add_header Cache-Control "no-cache";
    }

    # Flutter 主入口 - 短期缓存（可能更新）
    location = /main.dart.js {
        add_header Cache-Control "public, max-age=3600, must-revalidate";
    }

    # WASM 必须使用 application/wasm，不能复用 JavaScript MIME。
    location ~* \.wasm$ {
        default_type application/wasm;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # CanvasKit 等内容寻址的 JS 资源可长期缓存；其余 JS 只短期缓存。
    location ~* ^/(canvaskit|assets)/.*\.js$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location ~* \.js$ {
        add_header Cache-Control "public, max-age=3600, must-revalidate";
    }

    # 字体文件 - 长期缓存
    location ~* \.(woff|woff2|ttf|eot)$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        add_header Access-Control-Allow-Origin "*";
    }

    # 图片资源 - 长期缓存
    location ~* \.(png|jpg|jpeg|gif|ico|svg|webp)$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # CSS 文件 - 长期缓存
    location ~* \.css$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
